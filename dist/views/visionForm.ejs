<%- include('./header.ejs') %>
    <main class="add-vision-page">
      <h2>
        <%= title %>
      </h2>
      <div class="form-container">
        <form action="" method="post" class="add-vision">
          <div class="name-input">
            <label for="visionName">
              Vision Name:
            </label>
            <% if (typeof errors !== "undefined") { %>
              <% errors.forEach(error => { %>
                <% if (error.path === "name") { %>
                  <span class="error">
                    <%= error.msg %>
                  </span>
                <% } %>
              <% }) %>
            <% } %>
            <input
            type="text"
            id="visionName"
            name="name"
            required="true"
            value="<%= typeof vision === "undefined" ? "" : vision.name %>"
            >
        
          </div>
          <div class="desc-input">
            <label for="description">
              Description:
            </label>
            <% if (typeof errors !== "undefined") { %>
              <% errors.forEach(error => { %>
                <% if (error.path === "description") { %>
                  <span class="error">
                    <%= error.msg %>
                  </span>
                <% } %>
              <% }) %>
            <% } %>
            <textarea
              name="description"
              id="description"
              cols="30"
              rows="10"
              required="true"
            ><%= typeof character === "undefined" ? "" : vision.description %></textarea>
        
          </div>
          <div class="form-btns">
            <button type="submit">Submit</button>
            <button type="button" onclick="history.back()">Cancel</button>
          </div>
        </form>
      </div>
    </main>
<%- include('./footer.ejs') %>