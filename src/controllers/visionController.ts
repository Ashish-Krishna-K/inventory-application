import { NextFunction, Request, Response } from 'express';

export const allVisionsGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement all visions get
};

export const singleVisionCharactersGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement single vision characters get
};

export const addVisionGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement add Vision get
};

export const addVisionPost = async (req: Request, res: Response, next: NextFunction) => {
  // Implement add Vision post
};

export const editVisionGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement edit Vision get
};

export const editVisionPost = async (req: Request, res: Response, next: NextFunction) => {
  // Implement edit Vision post
};

export const deleteVisionGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement delete Vision get
};

export const deleteVisionPost = async (req: Request, res: Response, next: NextFunction) => {
  // Implement delete Vision post
};
