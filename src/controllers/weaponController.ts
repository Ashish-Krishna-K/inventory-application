import { NextFunction, Request, Response } from 'express';

export const allWeaponsGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement all Weapons get
};

export const singleWeaponCharactersGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement single Weapon characters get
};

export const addWeaponGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement add Weapon get
};

export const addWeaponPost = async (req: Request, res: Response, next: NextFunction) => {
  // Implement add Weapon post
};

export const editWeaponGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement edit Weapon get
};

export const editWeaponPost = async (req: Request, res: Response, next: NextFunction) => {
  // Implement edit Weapon post
};

export const deleteWeaponGet = async (req: Request, res: Response, next: NextFunction) => {
  // Implement delete Weapon get
};

export const deleteWeaponPost = async (req: Request, res: Response, next: NextFunction) => {
  // Implement delete Weapon post
};
